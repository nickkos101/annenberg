<section>
    <div class="container">
        <div class="grid">
            {% for product in collections.all-paintings.products limit: 20 %}
            {% render 'product-item' product: product %}
            {% endfor %}
        </div>
    </div>
</section>
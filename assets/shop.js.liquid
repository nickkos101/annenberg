jQuery(function($) {
  
  var hpSwiper = new Swiper(".mySwiper", {
    effect: "coverflow",
    grabCursor: true,
    centeredSlides: true,
    slidesPerView: "auto",
    coverflowEffect: {
      rotate: 75,
      stretch: 0,
      depth: 100,
      modifier: 1,
      slideShadows: false,
    },
    pagination: {
      el: ".swiper-pagination",
    },
  });
  
  
  var productSwiper = new Swiper(".product-images", {
    slidesPerView: "auto",
    centeredSlides: true,
    spaceBetween: 30,
    pagination: {
      el: ".swiper-pagination",
      clickable: true,
    },
  });
  
  // $('.product-item').hover(
  //   function() {
  //     var image = $(this).find('img');
  //     image.attr('src', image.attr('data-hover'));
  //   }, function() {
  //     var image = $(this).find('img');
  //     image.attr('src', image.attr('data-src'));
  //   });
    
    $( "td" ).hover(
      function() {
        $( this ).addClass( "hover" );
      }, function() {
        $( this ).removeClass( "hover" );
      }
      );
      
    });
    
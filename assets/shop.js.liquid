jQuery(function($) {
  
  var hpSwiper = new Swiper(".mySwiper", {
    effect: "coverflow",
    grabCursor: true,
    centeredSlides: true,
    slidesPerView: "auto",
    coverflowEffect: {
      rotate: 75,
      stretch: 0,
      depth: 100,
      modifier: 1,
      slideShadows: false,
    },
    pagination: {
      el: ".swiper-pagination",
    },
  });
  
  
  var productSwiper = new Swiper(".product-images", {
    slidesPerView: "auto",
    centeredSlides: true,
    spaceBetween: 30,
    pagination: {
      el: ".swiper-pagination",
      clickable: true,
    },
  });
  
  $('.product-item').hover(
    function(){
      var img_tag = $(this).find('img');
      var original_image = img_tag.attr('src');
      var hover_image = img_tag.attr('data-hover');
      img_tag.attr('src', hover_image);
    }, function(){
      var img_tag = $(this).find('img');
      var original_image = img_tag.attr('src');
      var hover_image = img_tag.attr('data-hover');
      img_tag.attr('src', original_image);
    });
    
    $( "td" ).hover(
      function() {
        $( this ).addClass( "hover" );
      }, function() {
        $( this ).removeClass( "hover" );
      }
      );
      
    });
    